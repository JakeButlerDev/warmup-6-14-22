isEqual = (a, b) => {     // Define function and parameters
    if (a == b) {   // Conditional logic if numbers are the same
        return true 
    } else return false // Otherwise return false
}
// Test output
console.log(isEqual(-2, -2));