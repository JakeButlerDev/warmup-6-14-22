reverseValue = (bool) => {  // Create function, pass parameter
    // Create logic to reverse whatever value is passed
    if (bool == true) {
        bool = false
    }
    else if (bool == false) {
        bool = true
    }
    // Return redefined value
    return bool
}
// Test output for functionality
console.log(reverseValue(true));